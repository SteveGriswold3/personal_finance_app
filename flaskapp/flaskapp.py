"""personal finance app"""
print("Pretend this is running for now.")
# TODO: build the flaskapp.
print("A journey of a thousand miles begins with a single step. ~Someone Smarter then Me.")

import json
from flask import Flask, request, render_template

app = Flask(__name__)

if __name__ == "__main__":
    #app.run(host='0.0.0.0', port='8082', debug=True)
    pass